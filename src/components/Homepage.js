import Header from './Header';
import Form from './Form';

const Homepage = () => {
    return(
        <main>
            <Header />
            <Form />
        </main>
    )
}

export default Homepage;